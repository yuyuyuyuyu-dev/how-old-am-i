# Project Instructions

- Always run the formatter after making code changes.
- Always remove unused code from the project. You can run `./gradlew detekt` to automatically detect unused imports, variables, and private declarations.
